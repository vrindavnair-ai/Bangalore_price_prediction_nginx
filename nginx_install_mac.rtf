#Steps to install Nginx on Mac
#check if brew is installed

brew

#if brew is available install nginx

brew install nginx

#cd to config file path
cd /opt/homebrew/etc/nginx

#update the config file 
#change the root to our file location and change the html file name
vi nginx.conf

#start nginx, if it is not working go for restart
brew services start nginx
brew services restart nginx
